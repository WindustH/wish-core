# Wish documentation

## Using Wish

- [Configuration](configuration.md): the `config.json` file, providers,
  session defaults, proxy, shell and secrets.
- [Deployment](deployment.md): running Wish as a service, access control,
  remote access, restarts, backups and upgrades.
- [HTTP API](api.md): every endpoint, the event streams and error handling,
  for building your own client.

The web and mobile client has its own documentation in the
[wish-web repository](https://github.com/WindustH/wish-web/blob/master/docs/en/README.md).

## Contributing

- [Internals](internals/README.md): how the engine is put together (sessions,
  the executor, compaction, history, storage, tools and the provider protocol
  layer).
